# CLAUDE.md

本文件为 Claude 在本仓库工作时提供指引。

## 项目目的

本仓库用于**整理 AI 相关文档的中文翻译**，核心理念是「**边翻译边学习**」。
不仅要产出准确的译文，更要在翻译过程中沉淀对 AI 概念、术语和技术细节的理解。

## 目录结构约定

顶层**只分两大类**：

- `official/`：**官方文档**。下面**必须再按平台细分**，一个平台一个子目录，例如 `official/anthropic/`、`official/openai/`、`official/langchain/`。
- `others/`：**其他**，涵盖博客、论文等非官方来源，可视需要再分 `others/blogs/`、`others/papers/` 等。

### 单篇文档

零散的单篇文档，在对应分类下单独建一个子目录，目录内包含：

```
official/<平台>/<文档主题>/     # 或 others/<子类>/<文档主题>/
├── original.md      # 原文（英文或其他语言）
├── cn.md            # 中文译文
├── glossary.md      # 本文出现的术语对照表（可选，术语多时使用）
└── notes.md         # 学习笔记：难点、背景知识、延伸理解
```

### 成套文档站点（多页面）

翻译一整套文档站点（如 MCP 官方文档）时，**按站点自身的导航层级镜像建目录**，用「数字前缀 + 空格」命名以保留原有顺序、并与站点标题保持一致。每个页面对应两个文件：原文 `<页面名>.md` 与译文 `<页面名>.cn.md`；页面用到的图片放同级 `images/` 子目录，在 md 中以相对路径引用。示例：

```
official/anthropic/mcp/
└── 1 Documentation/
    ├── 1 Get Started/
    │   ├── 1 What is MCP.md        # 英文原文
    │   ├── 1 What is MCP.cn.md     # 中文译文
    │   └── images/
    │       └── mcp-simple-diagram.webp
    ├── 2 About MCP/
    │   ├── 1 Architecture.md
    │   ├── 1 Architecture.cn.md
    │   └── ...
    └── ...
```

### 命名规则

- **单篇文档**目录与文件用小写、连字符分隔、避免空格（如 `prompt-caching/`）。
- **成套文档站点**的目录与页面文件沿用站点导航的原始标题，用「数字前缀 + 空格」命名（如 `1 Get Started/`、`1 What is MCP.md`），以对齐站点结构。
- 译文文件在原文文件名基础上加 `.cn.md` 后缀（如 `1 What is MCP.md` → `1 What is MCP.cn.md`）。
- 图片等资源放同级 `images/` 子目录，md 中用相对路径引用，不内嵌 base64。

## 翻译约定

- **忠实优先，通顺其次**：先保证技术含义准确，再打磨中文表达。有歧义时在 `notes.md` 记录取舍理由。
- **术语一致性**：同一术语在全仓库保持统一译法。首次出现时用「中文（English）」形式标注，之后可视情况只用中文。
- **标题必须中英对照**：正文中每一级标题（`#`~`######`，包括原文中用 `<Step title="...">` 等组件属性承担标题作用的情形，译文中已转换为标准标题语法，见下一条）翻译成中文后，统一在末尾加「（English）」标注原文标题，格式为「中文标题（English Title）」。仅普通段落或列表项中的术语沿用上一条「首次出现标注」的规则，无需每次重复。
- **`<Steps>`/`<Step title="...">` 转换为标准标题**：原文中用 `<Steps>` 包裹的一组 `<Step title="...">` 步骤块，本质上承担的是标题作用，但普通 Markdown 渲染器不识别这类 MDX 组件标签。因此在译文 `.cn.md` 中，应将 `<Steps>`/`</Steps>` 包裹标签整体去掉，把每个 `<Step title="...">` 转换成对应层级的标准 Markdown 标题（如 `#### 3.1.1 发现（Discovery）`），并去掉对应的 `</Step>` 闭合标签；原本因 JSX 嵌套产生的缩进也要一并去除，恢复为顶格的正文。原文 `.md` 保持不变，仍用 `<Steps>`/`<Step>` 标签。
- **`<CodeGroup>` 转换为列表 + 代码块**：原文中用 `<CodeGroup>` 包裹的一组带标题的代码块（fence 信息行形如 `` ```json Discover Request theme={null} ``），同样是普通 Markdown 不识别的 MDX 组件。译文 `.cn.md` 中应去掉 `<CodeGroup>`/`</CodeGroup>` 标签，把每个带标题的代码块转换成一个列表项：标题作为 `* 标题` 一行，空一行后缩进两格给出去掉了标题与 `theme={null}` 的纯代码块（语言标记保留，如 `` ```json ``），例如：

  ```
  * Discover Request

    ```json
    { ... }
    ```
  ```

  多个代码块依次转换成连续的列表项，无需保留分组标签。若某个代码块本身没有标题（fence 信息行只有 `theme={null}`，没有标题文字），则不转换为列表项，只需去掉 ` theme={null}` 后缀，保留原本的纯代码块。原文 `.md` 保持不变，仍用 `<CodeGroup>` 与带 `theme={null}` 的 fence 信息行。
- **标题需加层级编号**：仅在译文 `.cn.md` 中给标题加编号，原文 `.md` 保持不变。全文顶级的 `#` 一级标题是文档标题本身，**不加编号**；编号从第一个 `##` 二级标题开始，记为 `1`，其下 `###` 三级标题为 `1.1`、`1.2`……`####` 四级标题为 `1.1.1`、`1.1.2`……以此类推逐级细分。由 `<Step title="...">` 转换而来的标题，按其在文档中的实际层级参与编号。编号紧跟在 `#` 之后、标题文字之前，可与「（English）」标注同时使用，例如「## 2 MCP 的核心概念（Concepts of MCP）」「### 2.1 参与方（Participants）」。**代码块内的 `#`**（如 Python/Shell 注释、Markdown 示例代码）**不算标题，绝不参与编号**，编号只针对正文中真实的 Markdown 标题标记。
- **保留原文结构**：标题层级、代码块、公式、引用、链接原样保留，不擅自增删；`<Steps>`/`<Step>` 转标准标题（见上一条）是唯一的例外。
- **不确定的地方**：不要臆造。用 `<!-- TODO: ... -->` 或在 `notes.md` 标注疑问，供人工复核。
- **代码与命令**：代码、变量名、命令行、API 名称保持原文不译，仅翻译注释与说明文字。

## 学习笔记约定（notes.md）

翻译过程中遇到的新概念、背景知识、容易混淆的点，随手记录在对应文档的 `notes.md`：

- 核心概念的自我解释（用自己的话复述）
- 术语的由来与相关概念的区别
- 值得进一步查阅的引用或延伸阅读

## Claude 的工作方式

- 用**中文**与用户交流。
- 翻译新文档时，先确认原文来源与目标目录，再按上述结构创建文件。
- 修改已有译文时，保留原文件，逐段对照修改。
- 如需查证术语的通用译法或背景知识，可使用网络搜索。
